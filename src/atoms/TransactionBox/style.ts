import { StyleSheet } from "react-native";

export const TransactionsBoxStyle = StyleSheet.create({
    container: {
        display: 'flex',
        gap: 2
    },
    simpleText: {
        fontSize: 18,
        fontWeight: '600' 
    }
});