import { StyleSheet } from "react-native";

export const HomepageStyle = StyleSheet.create({
    container: {
      padding: 32,
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      gap: 32
    },
});