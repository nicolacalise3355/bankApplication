import { StyleSheet } from "react-native";

export const HomepageStyle = StyleSheet.create({
    container: {
      padding: 32,
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      gap: 16
    },
    primaryBtn: {
      backgroundColor: 'rgb(110, 201, 255)',
      padding: 16,
      borderRadius: 16
    }
});